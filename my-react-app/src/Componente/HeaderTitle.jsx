
const HeaderTitle = ({titulo, parrafo}) => {

    return(
    <div className="clsHeaderTitle">
        <h1>{titulo}</h1>
        <h2>{parrafo}</h2>
    </div>
    );    
    
}

export default HeaderTitle;